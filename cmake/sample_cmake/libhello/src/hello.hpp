#pragma warning(push, 0)
#include <string>
#pragma warning(pop)

namespace hello
{
    class SampleLib1
    {
    public:
        SampleLib1();
        std::string getName();
        std::string getFullName();
    };
}
